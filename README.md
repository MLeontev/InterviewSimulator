# InterviewSimulator

InterviewSimulator - это онлайн-платформа для симуляции технических собеседований разработчиков.
Платформа позволяет кандидатам проходить пробные собеседования для проверки теоретических знаний и навыков написания кода
и получать обратную связь с оценками и рекомендациями по дальнейшей подготовке.

---

## Возможности платформы

*   **Гибкий подбор заданий:** Прохождение собеседований по разным направлениям, стекам и грейдам с помощью выбора готовых пресетов.
*   **Два типа заданий:** Теоретические вопросы с развернутым текстовым ответом и алгоритмические задачи.
*   **Встроенный редактор кода:** Написание и запуск кода в браузере.
*   **Тестирование решений:** Автоматическая проверка кода на тест-кейсах с замером времени выполнения и потребления памяти.
*   **AI-оценка:** Оценка ответов и итоговый отчёт генерируются языковой моделью — с фидбеком по каждому ответу, разбором сильных и слабых сторон и рекомендациями по подготовке.
*   **Ограничение по времени:** Общий таймер на сессию с автоматическим сохранением ответов и завершением интервью при истечении времени.

---

## Процесс прохождения интервью

1.  **Выбор пресета:** Пользователь запускает сессию собеседования, выбирая пресет под свой технологический стек и грейд. Далее система подбирает набор заданий.
2.  **Выполнение заданий:** Кандидат последовательно выполняет задания в течение ограниченного времени:
    *   **Теоретические вопросы:** Пишет развернутые ответы.
    *   **Алгоритмические задачи:** Пишет код во встроенном редакторе.
3.  **Автопроверка кода:** При отправке кода решение автоматически прогоняется через тест-кейсы. После проверки кандидат получает вердикт.
4.  **Завершение сессии:** Сессия завершается при выполнении всех заданий, вручную кандидатом или автоматически по истечении таймера.
5.  **Генерация итогового отчета:** После завершения интервью формируется структурированный отчет:
    *   Общий балл и вердикт по собеседованию.
    *   Разбор каждого ответа с оценкой и фидбеком.
    *   Список сильных и слабых сторон, а также рекомендации по подготовке.

---

## Технологический стек

*   **Бэкенд:** .NET 10, C#, ASP.NET Core, Entity Framework Core, MediatR, MassTransit, Docker.DotNet
*   **Фронтенд:** React 19, TypeScript, Vite, Tailwind CSS, Monaco Editor, React Router, Axios
*   **Инфраструктура:** PostgreSQL, Keycloak, Nginx, GigaChat API, Docker Compose

---

## Структура репозитория

```text
├── backend/                       # Бэкенд (модульный монолит)
│   ├── src/                       
│   │   ├── Interview/             # Модуль управления собеседованиями
│   │   ├── CodeExecution/         # Модуль выполнения кода в Docker
│   │   ├── QuestionBank/          # Модуль банка заданий
│   │   ├── Users/                 # Модуль управления пользователями
│   │   ├── Framework/             # Переиспользуемые компоненты
│   │   └── InterviewSimulator.API/# Главный запускаемый веб-API проект
│   └── tests/                     # Тесты
│
├── frontend/                      # Клиентское React-приложение
├── infra/                         # Настройки инфраструктуры (PostgreSQL, Keycloak, Docker Compose)
└── docs/                          # Документация
```

---

## Локальный запуск

Для запуска проекта и инфраструктуры (бэкенд, фронтенд, база данных и Keycloak) используется Docker Compose.

### Предварительные требования
*   Docker и Docker Compose.
*   Данные (Client ID и Client Secret) для доступа к GigaChat API.

### Шаги для запуска

1.  **Настройка переменных окружения:**
    Создайте файл конфигурации `.env.local` в директории `infra/env/` на основе примера:
    ```bash
    cp infra/env/.env.local.example infra/env/.env.local
    ```
    Укажите в нем:
    *   `GIGACHAT_CLIENT_ID` и `GIGACHAT_CLIENT_SECRET` — ключи доступа к GigaChat.
    *   `HOST_CODE_EXECUTION_ROOT` — укажите абсолютный путь к папке `backend/.code-exec-tmp` на вашей локальной машине (например, `/Users/lmax/RiderProjects/InterviewSimulator/backend/.code-exec-tmp`).

2.  **Запуск контейнеров:**
    Запустите сборку и старт сервисов из корневой директории проекта:
    ```bash
    docker compose -f infra/docker/compose.yml -f infra/docker/compose.local.yml --env-file infra/env/.env.local up -d --build
    ```

3.  **Доступ к сервисам:**
    После старта всех контейнеров приложения будут доступны по следующим адресам:
    *   **Фронтенд (React App):** `http://localhost:3000`
    *   **Бэкенд Web API (Swagger):** `http://localhost:8090/swagger`
    *   **Панель администрирования Keycloak:** `http://localhost:8082` (логин/пароль по умолчанию: `admin` / `admin`)
---

## Тестирование

Тесты можно запустить как через IDE (VS, Rider), а также через консоль.

### 1. Юнит-тесты домена
```bash
dotnet test backend/tests/Interview.Domain.Tests
dotnet test backend/tests/CodeExecution.Domain.Tests
```

### 2. Интеграционные тесты
```bash
dotnet test backend/tests/Backend.IntegrationTests --filter "Category!=Docker"
```

### 3. Тесты Docker-песочницы
```bash
dotnet test backend/tests/Backend.IntegrationTests --filter "Category=Docker"
```